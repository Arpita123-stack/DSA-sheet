class Solution {
    public int sumBase(int n, int k) {
        int sum = 0;

        while (n > 0) {
            sum += n % k;  // last digit in base k
            n = n / k;     // remove last digit
        }

        return sum;
    }
}
